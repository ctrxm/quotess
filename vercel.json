{
  "buildCommand": "npx vite build && npx esbuild api/_source.ts --bundle --platform=node --format=cjs --target=node18 --outfile=api/server.js --packages=external",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/server" }
  ]
}
