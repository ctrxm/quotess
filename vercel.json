{
  "buildCommand": "npx vite build && npx esbuild api/_source.ts --bundle --platform=node --format=cjs --target=node18 --outfile=api/index.js --packages=external",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
