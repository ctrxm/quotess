{
  "buildCommand": "npx vite build && npx esbuild api/_source.ts --bundle --platform=node --format=esm --target=node18 --outfile=api/index.js --packages=external",
  "outputDirectory": "dist/public",
  "functions": {
    "api/index.js": {
      "memory": 512,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
